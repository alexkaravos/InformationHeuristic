defaults:
  - /datasets: cifar10Unbal
  - /models: resnet18
  - /representations: barlowtwin
  - _self_

experiment_name: "cifar10Unbal_barlowtwin"
device: "mps"
seed: 42
save_dir: "weights/cifar10Unbal/representations/barlowtwin/"
augmentation: cifar_aug1

models:
  in_channels: 3

representations:
  epochs: 1000
  batch_size: 1000
  lambda_param: 5e-3
