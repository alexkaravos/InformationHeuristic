defaults:
  - /datasets: cifar20
  - /models: resnet18
  - /representations: simclr
  - _self_

experiment_name: "cifar20_simclr"
device: "mps"
seed: 42
save_dir: "weights/cifar20/representations/simclr/"
augmentation: cifar_aug1

models:
  in_channels: 3

representations:
  temperature: 0.4
  epochs: 1000
  batch_size: 1000
